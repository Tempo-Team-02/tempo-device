# tempo-device
The actual final code for our project is named "TempoDevice.ino" anything else referring to final or new code is just test code that we didn't have an original name for. There are files that are generic names but they are also test code for sensors/microSD.
